#include <stdio.h>

int main(){
    int r=6;
    int h=10;
    printf("Area of circle with radius %d= %.3f\n", r,3.14*r*r);
    printf("Volume of cylinder with radius %d and height %d is %f:\n", r, h, 3.14*r*r*h);
    return 0;
}