#include <stdio.h>
int main() {
    int length, breadth, area;
    printf("The value for the rectangle are: \n");
    scanf("%d %d\n",&length,&breadth);
    printf("The area of the rectangle with the values given by you is= %d\n", area);
    area=length*breadth;
    
}